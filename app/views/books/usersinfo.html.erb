<h3>投稿者一覧</h3><br><br>

<table class="table table-striped">
	<tr>
		<th>プロフィール写真</th>
		<th>名前</th>
	</tr>

	<% @users.each do |user| %>
	<tr>
		<td><%= attachment_image_tag user, :image, :fill, 30, 30, fallback: "no_image.jpg" %></td>

		<td><%= user.name %></td>

		<td><%= link_to "表示", usershow_path(user.id) %></tr>

	<% end %>
</table>